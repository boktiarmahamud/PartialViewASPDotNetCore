# Contributing

- Follow .editorconfig rules.
- Create feature branches from `main`.
- Open a PR with description and link to issue.
- CI must pass before merge.
- Run `dotnet build` and `dotnet test` locally.